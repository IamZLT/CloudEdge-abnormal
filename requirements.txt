# Prefer: conda activate dinov3
# Already installed there: anomalib 2.5 / openvino / onnxruntime / mlflow / onnx
torch>=2.2
torchvision>=0.17
anomalib>=2.5
openvino>=2024.0
onnxruntime>=1.17
onnx>=1.14
mlflow>=2.10
pyyaml>=6.0
tqdm>=4.65
psutil>=5.9
scikit-learn>=1.2
Pillow>=9.0
numpy>=1.23

# Optional: unified VLM backends (use: conda activate cloudedge)
# Qwen3.5 support requires the current Transformers 5.x model definitions.
transformers>=5.0
accelerate>=1.0
qwen-vl-utils>=0.0.14
bitsandbytes>=0.49
modelscope>=1.39
einops>=0.8
timm>=1.0
sentencepiece>=0.2
# peft is needed only when loading a LoRA adapter.
peft>=0.17
fastapi>=0.110
uvicorn>=0.27
python-multipart>=0.0.9
