protocol:
  seed: 42
  image_size: 224
  max_test_per_category: null
  max_train_normal: null
  calibration_fraction: 0.2
  normal_threshold_quantile: 0.99
  traditional_batch_size: 8
  num_workers: 2

datasets:
  mvtec: [all]

traditional:
  method: patchcore_resnet18
  backbone: resnet18
  layers: [layer2, layer3]
  coreset_ratio: 0.1
  max_memory_bank: 10000
