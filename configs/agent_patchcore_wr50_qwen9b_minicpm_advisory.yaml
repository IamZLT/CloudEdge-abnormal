v1_root: outputs/detection_agent_wr50_b10_mvtec

verifier:
  model_id: minicpm_v_4_5_int4
  backend: minicpm
  model_path: model_card/MiniCPM-V-4_5-int4
  dtype: bfloat16
  max_new_tokens: 160
  min_confidence: 0.90
  require_region_agreement: true
  # No automatic override is justified by the current development-set result.
  # Preserve WR50 decisions while retaining VLM evidence for audit/human review.
  allow_overrides: false

protocol:
  version: heterogeneous_advisory_v2
  test_labels_used_for_inference: false
  note: enable automatic overrides only after unchanged external validation
